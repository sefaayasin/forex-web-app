streamlit>=1.40,<2
pandas>=2.2,<3
numpy>=1.26,<3
plotly>=5.24,<7
pytz>=2024.1
yfinance>=0.2.54,<0.3
# Must match the version data/ml/tournament/*.joblib was saved with, or
# unpickling breaks on the host (scikit-learn does not guarantee pickle
# compatibility across versions). Retrain and re-save those files if you
# change this pin.
scikit-learn==1.9.1
joblib>=1.3
# Optional broker integration (install only on a machine with MetaTrader 5):
# MetaTrader5>=5.0.45
